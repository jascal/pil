# Paper: *Projective Incidence Calculus for Transformer Decision Logic*

ACM `sigconf` submission for the `pil` project. Every quantitative claim is
transcribed from a checked-in result file under [`../results/`](../results) — see
the provenance table below.

## Files

| file | what |
|---|---|
| `pic.tex` | the paper (acmart / sigconf) |
| `references.bib` | bibliography (external entries flagged for verification) |
| `make_figures.py` | regenerates the four **data** figures from `../results/` numbers |
| `figures/*.pdf` | the four data figures (PDF for LaTeX, PNG for preview) |

(Figure 1, the Cell-Capacity-vs-TRC schematic, is drawn inline with TikZ in `pic.tex`
— it is conceptual, not data-driven, so it is not produced by `make_figures.py`.)

## Build

Needs a TeX distribution with `acmart.cls` (TeX Live `texlive-publishers`, or
MiKTeX). **Not installed in this environment** — build elsewhere:

```bash
python paper/make_figures.py          # (re)generate figures/*.pdf  [needs ../.venv]
cd paper
pdflatex pic && bibtex pic && pdflatex pic && pdflatex pic
```

For double-blind review, change the class line to
`\documentclass[sigconf,anonymous,review]{acmart}` and drop `nonacm`. For
camera-ready, drop `nonacm` and add the venue's `\acmConference`, `\setcopyright`,
and `\acmDOI`.

## Claim → result-file provenance

| paper element | source |
|---|---|
| frame_reg null | `results/hard_sweep.txt`, `results/frame_reg_surface.txt` |
| generation lever (Fig. `generation_lever`) | `results/comp_sweep.txt` |
| selection vs training 2×2 | `results/scored2_sweep.txt` |
| capacity slack (~1e59) | `results/capacity_diagnostic.txt` |
| M-dominance (Fig. `m_dominance`) | `results/routing_complexity_sweep.txt` |
| interference in ℝ^M | `results/interference_probe.txt`, `results/coherence_reg.txt` |
| rank vs scale, Qwen (Fig. `rank_concentration`) | `results/real_dla_sweep.txt` |
| consistent late-MLP circuit (Qwen) | `results/decode_circuit.txt` |
| **Pythia scale + circuit** | `results/real_dla_pythia.txt` |
| coherence: Qwen vs Pythia (Fig. `coherence_slack`) | `results/real_dla_sweep.txt` + `results/real_dla_pythia.txt` |
| difficulty dial | `results/real_dla_sweep.txt` |
| multilingual (Appendix A) | `results/real_dla_sweep.txt` |
| Theorems (softmax / separation / rank / Welch) | machine-checked in `i-orca` (cited) |

Pythia numbers are regenerated by running the existing analyses on the
`/tmp/d_pythia*.jsonl` dumps:
`for m in 70m 160m 410m 1b; do .venv/bin/python experiments/real_dla_analysis.py /tmp/d_pythia$m.jsonl; done`
(the captured output is checked in as `results/real_dla_pythia.txt`).

## Headline cross-family finding

Rank concentration on a stable late-MLP circuit transfers to **both** Qwen2.5 and
Pythia. Coherence packing does **not**: Qwen sits 2–3× above the Welch floor, Pythia
packs near it (1.06–1.5×). At matched dimension (`nb=49`), Pythia-410M (1.38×) is far
tighter than Qwen2.5-0.5B (2.01×) — so near-optimal packing is family-dependent
(a training-recipe property), not synthetic-specific.
